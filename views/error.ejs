<%- include('partials/header') %>
<div class="d-flex align-items-center justify-content-center min-vh-100 bg-light">
    <div class="text-center p-5 bg-white rounded-4 shadow-sm border" style="max-width: 500px;">
        <i class="fa-solid fa-triangle-exclamation text-warning display-1 mb-3"></i>
        <h2 class="fw-bold mb-2">Access Denied / Error</h2>
        <p class="text-muted mb-4"><%= typeof message !== 'undefined' ? message : 'An unexpected error occurred.' %></p>
        <a href="/" class="btn btn-emerald px-4">Return to Home</a>
    </div>
</div>
<%- include('partials/footer') %>
